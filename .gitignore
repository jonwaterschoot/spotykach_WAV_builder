# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env
.env.local
Thumbs.db
*.private.md


# Windows folder settings
desktop.ini

# The backup the import script writes before it edits the manifest — it exists for
# the minute between applying an import and reading `git diff`. Staged audio does not
# appear here at all: it goes to a working folder outside the repo, on purpose.
public/manifest.json.bak
